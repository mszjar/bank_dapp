Launch the blockchain on hardhat
```shell
// /backend
yarn hardhat node
```

Deploy the contract on localhost (open new terminal)
```shell
// /backend
yarn hardhat run scripts/deploy.js --network localhost
```

Launch the Next app
```shell
// /frontend
yarn dev
```
